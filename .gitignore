# Virtual environments
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Local secrets / config — never commit. dev.env holds a working operator
# token, so only the *.example templates are tracked.
.env
.env.*
*.env
!*.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store

# Captured device logs
*.log

# Attendance database — personal data, never commit
data/
*.db
*.db-wal
*.db-shm

# Identity mapping (phase 4)
directory.yaml
